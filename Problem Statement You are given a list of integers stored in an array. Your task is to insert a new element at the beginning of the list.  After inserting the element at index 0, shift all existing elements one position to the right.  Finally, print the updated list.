#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N, X;
    int arr[100];
    scanf("%d", &N);
    for(int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }
    scanf("%d", &X);
    for (int i=N; i>0; i--){
        arr[i] = arr[i - 1];
    }
    arr[0] = X;
    for(int i = 0; i <= N; i++) {
        printf("%d ", arr[i]);
    }
    return 0;
}
