#include <stdio.h>
#include <string.h>

int main() {
    char s[101];
    scanf("%s", s);

    printf("%c", s[0]);

    for (int i = 1; s[i] != '\0'; i++) {
        if (s[i] != s[i - 1]) {
            printf("%c", s[i]);
        }
    }

    return 0;
}
